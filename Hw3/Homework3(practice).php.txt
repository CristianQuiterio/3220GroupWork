<html>
	<?php
		$arrayFirst=[];
		$firstNames=fopen("first_names.csv","r");
		$fnames=fgets($firstNames);
		$arrayFirst=explode(",", $fnames);
	
		fclose($firstNames);
		
		$arrayLast=[];
		$lastNames=fopen("last_names.txt","r");
		for ($i = 0; $i < 24; $i++){
			$arrayLast[$i]=fgets($lastNames);
		}
		fclose($lastNames);
		print("<p>$arrayFirst[0]</p>");
		print("<p>$arrayLast[2]</p>");


		$arrayStreet=[];
		$streetNames=fopen("street_names.txt","r");
		for ($i = 0; $i < 6; $i++){
			$sNames=fgets($streetNames);
			array_push($arrayStreet, explode(":", $sNames));
		}
		fclose($streetNames);
		print("<p>$arrayStreet[0]</p>");


		$arrayType=[];
		$streetType=fopen("street_types.txt","r");
		for ($i = 0; $i < 1; $i++){
			$sType=fgets($streetType);
			$arrayType=explode("..;", $sType);
		}
		fclose($streetType);
		print("<p>$arrayType[0]</p>");

	?>
</html>
